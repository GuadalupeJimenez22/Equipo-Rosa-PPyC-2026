services:

  postgres-app:
    container_name: Programacion-Paralela-Concurrente-test
    image: postgres:17
    restart: always
    ports:
      - "5450:5432"   
    environment:  
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ppyc_db
    volumes:
      - postgres-app-volume:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    labels:
      - "com.relinter.service=postgres-app"
      - "com.relinter.role=database"
      - "com.relinter.backup=enabled"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ppyc_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-app-volume: